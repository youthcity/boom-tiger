import * as IGlobal from 'interfaces/global_interfaces';
import * as ICloudVar from 'interfaces/cloud_variable_interfaces';

// @autoinject(name=cloud_variable_service)
export function cloud_variable_service_factory(
  u:IGlobal.Util,
) : ICloudVar.Service {

  return {
  };
}
